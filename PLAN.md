# FTP 客户端实验计划书（Socket 方式）

## 1. 选题与目标

### 1.1 选题
本实验选择 **FTP 客户端** 方向。

### 1.2 总体目标
在 Windows 客户端上，使用 C# 通过 Socket 编程方式实现 FTP 协议核心功能，提供图形化界面，满足以下实验要求：

1. 具备图形化界面。
2. 支持文件上传与下载。
3. 支持断点续传（上传/下载均支持）。
4. 从创建 socket、建立 TCP 连接开始，按协议完成交互。

## 2. 需求拆解

### 2.1 功能需求
1. 连接管理：输入服务器地址、端口、用户名、密码；连接与断开。
2. 目录浏览：显示远程目录列表（文件名、大小、时间、类型）。
3. 上传功能：本地文件上传至远程目录。
4. 下载功能：远程文件下载至本地目录。
5. 断点续传：
   - 下载续传：支持从本地已有字节偏移继续下载。
   - 上传续传：支持从远程已有字节偏移继续上传。
6. 日志面板：显示关键命令/响应码（如 220、331、230、227、150、226、550）。

### 2.2 非功能需求
1. 可用性：常见异常（认证失败、路径错误、连接中断）需有明确提示。
2. 可维护性：网络协议层与界面层解耦。
3. 稳定性：大文件传输不崩溃，断网后可恢复或重新连接。

### 2.3 约束与假设
1. 客户端目标系统：Windows。
2. 编程语言：C#。
3. 协议实现方式：Socket 原生实现，不依赖高层 FTP 封装库。
4. 数据库：FTP 题目不强制数据库，本项目不引入数据库。

## 3. 技术方案

### 3.1 架构分层
1. UI 层（WinForms/WPF）：界面交互、参数输入、进度展示。
2. 应用层（Command/Service）：业务流程编排（连接、列表、上传、下载、续传）。
3. 协议层（FtpControl/FtpData）：
   - 控制连接：发送 FTP 命令、解析响应。
   - 数据连接：处理 PASV 模式的数据 socket。
4. 基础设施层：日志、文件分块读写、重试策略。

### 3.2 关键协议点
1. 登录：USER / PASS。
2. 工作目录：PWD / CWD。
3. 列目录：PASV + LIST。
4. 下载：TYPE I + PASV + REST + RETR。
5. 上传：TYPE I + PASV + REST + STOR。
6. 续传实现核心：REST <offset>。

### 3.3 模式选择
优先实现 **被动模式（PASV）**，以降低 NAT/防火墙下的连接复杂度。

## 4. 模块设计

### 4.1 控制连接模块
1. 建立 TCP 连接（默认 21）。
2. 行协议读写（CRLF 结尾）。
3. 多行响应解析（如 220-... / 220 ...）。
4. 响应码分类处理（2xx 成功，4xx/5xx 失败）。

### 4.2 数据连接模块
1. 解析 PASV 响应中的 IP/端口。
2. 建立数据 socket。
3. 与 RETR/STOR/LIST 协同关闭数据通道。

### 4.3 文件传输模块
1. 下载：流式写入本地文件；支持断点偏移。
2. 上传：流式读取本地文件；支持断点偏移。
3. 进度回调：已传输字节、速度、剩余时间（可选）。

### 4.4 UI 模块
1. 登录区：Host/Port/User/Password。
2. 文件区：本地文件浏览 + 远程目录列表。
3. 操作区：上传、下载、暂停/继续、删除（可选）。
4. 日志区：命令和响应可视化。

## 5. 里程碑与时间安排（建议 4 周）

### 第 1 周：协议基础与连接
1. 完成控制连接与响应解析。
2. 实现登录、PWD、CWD。
3. 命令行原型可连接并执行基本命令。

交付物：
1. Socket 控制通道代码。
2. 协议交互日志截图。

### 第 2 周：目录与基础传输
1. 实现 PASV + LIST。
2. 实现 RETR/STOR 基础传输（无续传）。
3. 完成异常处理框架。

交付物：
1. 可用的上传/下载原型。
2. 基础功能测试记录。

### 第 3 周：断点续传与稳定性
1. 接入 REST 偏移逻辑。
2. 下载续传、上传续传联调。
3. 增加断线重连与失败重试策略（基础）。

交付物：
1. 续传功能演示视频/截图。
2. 大文件测试报告。

### 第 4 周：图形化界面与验收
1. 完成 WinForms/WPF 界面。
2. 完成日志展示与交互优化。
3. 完成验收测试与文档整理。

交付物：
1. 可执行客户端（Windows）。
2. 项目报告、测试报告、使用说明。

## 6. 测试计划

### 6.1 功能测试
1. 正确登录/错误登录。
2. 列目录成功/目录不存在。
3. 上传成功/下载成功。
4. 覆盖已有文件的处理策略（覆盖、重命名、取消）。

### 6.2 续传测试
1. 下载中断后继续，文件 Hash 一致。
2. 上传中断后继续，远程文件完整。
3. 偏移点在 0、中间、接近末尾三种场景。

### 6.3 异常测试
1. 网络断开。
2. 权限不足（550）。
3. 磁盘空间不足。

## 7. 验收标准（与实验要求映射）

1. 图形化界面：可在 Windows 下运行并完成文件操作。
2. 上传下载：可稳定传输常见文件类型。
3. 断点续传：中断后可继续，最终文件完整。
4. Socket 编程：提交代码中可见从 socket 创建、TCP 连接到协议命令处理全过程。

## 8. 风险与应对

1. 协议细节复杂（多行响应/状态码时序）。
   - 应对：先完成最小命令集，使用抓包与日志对照调试。
2. 续传边界错误（偏移不一致）。
   - 应对：统一偏移计算，增加 Hash 校验。
3. Windows UI 与 Linux 开发环境差异。
   - 应对：协议层先在终端完成并充分测试，最后迁移到 Windows UI 层。

## 9. 当前执行策略

当前先在 Linux 下完成“终端版协议核心”，确保 Socket + FTP 功能可用；
随后在 Windows 上接入 WinForms/WPF 完成图形化与最终验收。
